name: Test Samples

on:
  push:
    branches:
      - main
      - develop

  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:

jobs:
  test-chat:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      
      - name: Setup test environment
        uses: ./.github/actions/setup-test-env
        with:
          enable-postgres: 'false'

      - name: Create coverage directory
        run: mkdir -p /tmp/coverage
      
      - name: Run chat client/server test with coverage
        env:
          GOCOVERDIR: /tmp/coverage
          ENABLE_COVERAGE: "true"
        run: |
          python3 tests/samples/chat/test_chat.py
      
      - name: Convert coverage data to text format
        run: |
          # We collect runtime coverage under a single directory (/tmp/coverage),
          # so we can convert it directly without a merge step.
          if [ -d /tmp/coverage ]; then
            go tool covdata textfmt -i=/tmp/coverage -o=coverage-chat.out
          else
            echo "No coverage data found at /tmp/coverage"; exit 0;
          fi

      - name: Upload coverage to Codecov
        if: ${{ hashFiles('coverage-chat.out') != '' }}
        uses: codecov/codecov-action@v5
        with:
          files: ./coverage-chat.out
          flags: chat-integration
          name: chat-integration
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

  test-chat-clustered:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      
      - name: Setup test environment
        uses: ./.github/actions/setup-test-env
        with:
          enable-postgres: 'false'
          enable-py-grpc: 'true'
      
      - name: Create coverage directory
        run: mkdir -p /tmp/coverage
      
      - name: Run chat client/server test with 4 servers and coverage
        env:
          GOCOVERDIR: /tmp/coverage
          ENABLE_COVERAGE: "true"
        run: |
          python3 tests/samples/chat/test_chat.py --num-servers 4
      
      - name: Convert coverage data to text format
        run: |
          # We collect runtime coverage under a single directory (/tmp/coverage),
          # so we can convert it directly without a merge step.
          if [ -d /tmp/coverage ]; then
            go tool covdata textfmt -i=/tmp/coverage -o=coverage-chat-clustered.out
          else
            echo "No coverage data found at /tmp/coverage"; exit 0;
          fi

      - name: Upload coverage to Codecov
        if: ${{ hashFiles('coverage-chat-clustered.out') != '' }}
        uses: codecov/codecov-action@v5
        with:
          files: ./coverage-chat-clustered.out
          flags: chat-clustered
          name: chat-clustered
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

  test-chat-race:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      
      - name: Setup test environment
        uses: ./.github/actions/setup-test-env
        with:
          enable-postgres: 'false'
      
      - name: Run chat client/server test with race detector
        env:
          ENABLE_RACE: "true"
        run: |
          python3 tests/samples/chat/test_chat.py

  test-counter:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      
      - name: Setup test environment
        uses: ./.github/actions/setup-test-env
        with:
          enable-postgres: 'false'

      - name: Create coverage directory
        run: mkdir -p /tmp/coverage
      
      - name: Run counter server test with coverage
        env:
          GOCOVERDIR: /tmp/coverage
          ENABLE_COVERAGE: "true"
        run: |
          python3 tests/samples/counter/test_counter.py
      
      - name: Convert coverage data to text format
        run: |
          # We collect runtime coverage under a single directory (/tmp/coverage),
          # so we can convert it directly without a merge step.
          if [ -d /tmp/coverage ]; then
            go tool covdata textfmt -i=/tmp/coverage -o=coverage-counter.out
          else
            echo "No coverage data found at /tmp/coverage"; exit 0;
          fi

      - name: Upload coverage to Codecov
        if: ${{ hashFiles('coverage-counter.out') != '' }}
        uses: codecov/codecov-action@v5
        with:
          files: ./coverage-counter.out
          flags: counter-integration
          name: counter-integration
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

  test-counter-clustered:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      
      - name: Setup test environment
        uses: ./.github/actions/setup-test-env
        with:
          enable-postgres: 'false'
      
      - name: Create coverage directory
        run: mkdir -p /tmp/coverage
      
      - name: Run counter server test with 3 servers and coverage
        env:
          GOCOVERDIR: /tmp/coverage
          ENABLE_COVERAGE: "true"
        run: |
          python3 tests/samples/counter/test_counter.py --num-servers 3
      
      - name: Convert coverage data to text format
        run: |
          # We collect runtime coverage under a single directory (/tmp/coverage),
          # so we can convert it directly without a merge step.
          if [ -d /tmp/coverage ]; then
            go tool covdata textfmt -i=/tmp/coverage -o=coverage-counter-clustered.out
          else
            echo "No coverage data found at /tmp/coverage"; exit 0;
          fi

      - name: Upload coverage to Codecov
        if: ${{ hashFiles('coverage-counter-clustered.out') != '' }}
        uses: codecov/codecov-action@v5
        with:
          files: ./coverage-counter-clustered.out
          flags: counter-clustered
          name: counter-clustered
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

  test-tictactoe:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      
      - name: Setup test environment
        uses: ./.github/actions/setup-test-env
        with:
          enable-postgres: 'false'

      - name: Create coverage directory
        run: mkdir -p /tmp/coverage
      
      - name: Run tictactoe server test with coverage
        env:
          GOCOVERDIR: /tmp/coverage
          ENABLE_COVERAGE: "true"
        run: |
          python3 tests/samples/tictactoe/test_tictactoe.py
      
      - name: Convert coverage data to text format
        run: |
          # We collect runtime coverage under a single directory (/tmp/coverage),
          # so we can convert it directly without a merge step.
          if [ -d /tmp/coverage ]; then
            go tool covdata textfmt -i=/tmp/coverage -o=coverage-tictactoe.out
          else
            echo "No coverage data found at /tmp/coverage"; exit 0;
          fi

      - name: Upload coverage to Codecov
        if: ${{ hashFiles('coverage-tictactoe.out') != '' }}
        uses: codecov/codecov-action@v5
        with:
          files: ./coverage-tictactoe.out
          flags: tictactoe-integration
          name: tictactoe-integration
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

  test-tictactoe-clustered:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      
      - name: Setup test environment
        uses: ./.github/actions/setup-test-env
        with:
          enable-postgres: 'false'
      
      - name: Create coverage directory
        run: mkdir -p /tmp/coverage
      
      - name: Run tictactoe server test with 3 servers and coverage
        env:
          GOCOVERDIR: /tmp/coverage
          ENABLE_COVERAGE: "true"
        run: |
          python3 tests/samples/tictactoe/test_tictactoe.py --num-servers 3
      
      - name: Convert coverage data to text format
        run: |
          # We collect runtime coverage under a single directory (/tmp/coverage),
          # so we can convert it directly without a merge step.
          if [ -d /tmp/coverage ]; then
            go tool covdata textfmt -i=/tmp/coverage -o=coverage-tictactoe-clustered.out
          else
            echo "No coverage data found at /tmp/coverage"; exit 0;
          fi

      - name: Upload coverage to Codecov
        if: ${{ hashFiles('coverage-tictactoe-clustered.out') != '' }}
        uses: codecov/codecov-action@v5
        with:
          files: ./coverage-tictactoe-clustered.out
          flags: tictactoe-clustered
          name: tictactoe-clustered
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

  test-tictactoe-race:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      
      - name: Setup test environment
        uses: ./.github/actions/setup-test-env
        with:
          enable-postgres: 'false'
      
      - name: Run tictactoe server test with race detector
        env:
          ENABLE_RACE: "true"
        run: |
          python3 tests/samples/tictactoe/test_tictactoe.py
